

  *** Selector ***

  ** Selector :- It's use to find the HTML element and select it.
     OR we can check the which event or action perform in this.

     -> It'a similar to select like in CSS.
